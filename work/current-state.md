# Current state — 2026-08-26

This is the source-first live-status overlay for the work record.

Use current implementation and retained current evidence as the authority for what exists and what has been demonstrated. README files, portfolio prose, issue summaries, and claim indexes are projections of that reality and should be updated when they lag it. For career/editorial context, read `portfolio-inventory.md` after this file.

## Preflight — ~101s → 13.69s development arc; candidate execution

Repository: https://github.com/teamleaderleo/preflight

State: **source and rendered-UI convergence complete; first-beta candidate execution active**.

The current development headline is **~101 seconds → 13.69 seconds** on the 83-mod M5 MacBook Air development installation: roughly 101 seconds is the retained early observed high end, while 13.69 seconds is the retained current development endpoint from the later G1/deferred-heap-commit condition. That JVM policy became the current macOS Rosetta path. Repeated current runs remain in the same low-14-second regime; they support the headline rather than replacing it.

Current source also includes the learned Compact texture path, physical-order-sensitive pack publication, desktop/navigation refinements, dependency/setup checks, manual privacy-bounded support reporting, signed update/package machinery, and release-scoped CI refinements.

Private signing/package rehearsals have succeeded across Linux, macOS, and Windows. Remaining first-beta work belongs to one maintainer-authorized immutable candidate generation: native Windows/Linux licensed-install acceptance, package-bound startup/lifecycle/update/report evidence, the hands-on report-intake canary, and the final publication decision.

## Glaeda — landed M6 hot-state substrate

Repository: https://github.com/teamleaderleo/smolrunner

State: **pre-alpha trust-tiered Linux execution in live Apple-silicon acceptance**.

Current main has moved beyond a disposable-runner controller and beyond a roadmap-only hot-execution concept. Landed M6 modules include bounded hot-execution performance receipts, hot-state path policy, crash-safe project-disk leases, trusted OverlayFS task views and mount plans, descriptor-bound mount prerequisites, immutable Git object-pool generations with consumer leases and fixed markers, and Git index stat patching for copy-on-write task materialization.

The strict disposable path remains substantial: prepared Lima/VZ workers, official GitHub Runner Scale Set integration, durable assignment/no-replay handling, clone/JIT/teardown composition, LaunchAgent supervision, controller-death recovery evidence, exact worker ownership, and physical Quarry pilots. The next product step is composing the landed M6 primitives into measured trusted task loops while continuing disposable-service, restart, recovery, and hostile-network work.

## Stensibly — live coordination plus repository-to-mail continuation

Repository: https://github.com/teamleaderleo/stensibly

State: **live hosted coordination/continuation system in ongoing dogfood**.

Current hosted implementation includes Convex-backed shared state, Cloudflare Worker REST/MCP, browser sessions and bearer clients, project/workspace scoping, durable claims/runs/reservations, idempotent writes, handoffs, and guarded exact-CAS GitHub publication.

The production Worker also has an automatic repository-attention → mail continuation path for the explicit Quarry dogfood mapping. Signed GitHub webhook observations and a bounded read-only public GitHub Events fallback feed the same durable observation, attention, thread, and Gmail publishing path. Public fallback observations carry explicit provenance; polling advances after downstream acknowledgement; stale current-head evidence stays quiet; the fallback adds no GitHub write authority. Outbound mail exposes a project-owned continuation code while internal thread/effect/provider identity remains stable for replay and reconciliation.

## Cultist — provider-bound repository evidence

Repository: https://github.com/teamleaderleo/cultist

State: **active repository-evidence research prototype in sustained dogfood**.

Current `cargo-cultist` remains deterministic, local, and read-only for its public analyzer commands. Landed source covers repository conventions, change-time evidence, concurrent-change preflight, historical companions, CI selector analysis, bounded packets, provenance, decision memory, and behavioral research.

The active-work path now binds provider-backed dogfood to explicit provider snapshot identity. Consumption revalidates the observed frontier, unresolved provider/coordination evidence stays `UNKNOWN`, pagination/partial reads cannot silently become complete coverage, and bounded file coverage is asserted only when one provider response proves the selected union.

## Proofwake — evidence index with evaluation projections

Repository: https://github.com/teamleaderleo/proofwake

State: **working local revision-evidence index; Proofwake is the primary product identity**.

Current main includes local Git and signed GitHub observation ingestion, a durable local ledger, deterministic repository/revision/activity projections, diagnostics/dashboard/reporting, and read-only MCP. The older Shadowbill AI-usage reckoner is now an optional compatibility module inside the wider product.

Evaluation evidence is implemented: strict work-evaluation and review-finding receipts can be appended, `proofwake evaluation` rebuilds one task-specific projection with current marks/findings, immutable histories, exclusions, coverage, confidence, uncertainty and limitations, and the same projection is available through read-only MCP. The projection carries no routing, approval, merge, deployment, or global scoring authority.

## Renderprove — browser evidence plus screenshot advisory contracts

Repository: https://github.com/teamleaderleo/renderprove

State: **early-stage working browser-evidence tool with several bounded evidence paths landed**.

Current code includes Chromium review receipts, screenshots/hashes/diagnostics, bounded local MCP, rootless-Podman renderer and repeatability probes, bounded interaction plans, deterministic PNG comparison, and the existing optional source-oriented Gemma advisory path.

The screenshot-oriented `vision-check` path is also implemented. It builds one bounded provider-neutral request from an explicit PNG, short operator brief and optional matching receipt; canonicalizes the image to deterministic bounded RGBA PNG; emits a privacy-safe preview; and binds the packet to fixed prompt/canonicalization identities plus a request digest. The repository also exports strict provider-free `vision-advice-v1` normalization/schema code. A live vision-provider adapter remains a later integration step.

## Quarry — private research code ahead of the old summary

Repository: private `teamleaderleo/quarry`.

State: **active private trading-research lab; live-order permission disabled**.

Current code includes substantially more than the older alpha/dogfood shorthand: immutable data/experiment identities, deterministic backtests and tournaments, causal execution policy, futures/options accounting, exact risk/restart state, verification receipts, read-only broker observation, offline statement evidence, continuation handoffs, and provider/data-fidelity work.

The frozen BTC hourly family has completed: momentum, channel breakout and short-term reversal retained rejected primary results, leaving no survivor and keeping ETH replication unopened. Current adaptive trend work has moved into a causal entry-state/scaling layer under the same research/promotion fences.

Market-data code has also advanced. Yahoo OHLCV parsing preserves provider fractional literals as exact `Decimal` values before normalized receipt hashing. The prospective BTC quote recorder consumes verified daily source evidence plus exact bounded Coinbase Advanced Trade `level2` message bytes, timestamps receipt/processing through its module-owned clock, and stamps every artifact with a fixed research-only authority block: no provider-current claim, account access, simulated fill, promotion/capital authority, or live-order permission.

## Elatura — active browser/device prototype

Repository: https://github.com/teamleaderleo/elatura

State: **active prototype and dogfood beyond observation-only M0**.

Current main includes observe-only Firefox measurement, a locked fail-open slim mode, bounded live DOM discovery/windowing/restoration, a preflighted DOM executor/browser host, local Android completion-notification sensing and diagnostics, generic adapter/conformance contracts, deterministic oversized fixtures, bounded representations, cache/provenance controls, and broader resource/offload experiments.

The active product question remains which intervention layer earns its complexity in real use: live page-state suppression/windowing, cheaper local representation, or offloaded execution while preserving the native service experience.

## Fieldwork and Linux Fieldwork — investigation engines

Repositories:

- https://github.com/teamleaderleo/fieldwork
- https://github.com/teamleaderleo/linux-fieldwork

State: **active code-first research workbenches**.

Their current READMEs already track implementation closely. Fieldwork organizes programmes, target hubs, scout lanes, experiments, integration trials, exact-head review and deliberate upstream delivery. Linux Fieldwork carries exact source imports, formal lanes, reproducible investigations, live-fieldwork status and reusable review/process lessons. Their strongest public value remains the accepted upstream work and retained investigations they produce.

## External upstream work

### Vercel AI SDK

State: **one direct merged/published repair plus two Factory-adopted repairs with retained co-author credit**.

This remains the strongest external application/tooling cluster in the work record.

### Vite

State: **two merged lifecycle/correctness fixes plus one open repeated-config-resolution idempotence follow-on**.

Transient GitHub mergeability belongs to the live upstream PR; the durable record should describe the technical/status boundary.

### Cloud Hypervisor

State: **four merged Rust/VMM fixes across lifecycle, boot errors, VFIO mapping, and QCOW metadata ownership**.

The merged work spans exact VMM shutdown completion, ACPI error propagation, sparse VFIO BAR mapping semantics, and QCOW L2 ownership/refcount ordering. PR #8721 merged on 2026-08-24 after moving new/replacement L2 ownership before L1 publication and keeping the relocation ownership handoff local rather than deferring it to later cleanup.

## FEX — owned-fork runtime research

Owned fork: https://github.com/teamleaderleo/FEX

State: **validated owned-fork runtime/ABI/Vulkan research**.

The retained work covers callback-sensitive dynamic Vulkan proc-address routing, native GIPA/GDPA availability semantics, generated-thunk executable lifetime across wrapper unload/reload, and metadata/inventory consistency checks. Keep contribution language tied to the actual upstream boundary and policy.

## Scrapbook / teamleaderleo.com

Repository: https://github.com/teamleaderleo/scrapbook

State: **live personal site, private knowledge workspace, and repository-backed publication/evidence lab**.

Current main uses Next.js 16.3.2 / React 19.2.1. Routine hosted CI is ESLint + Vitest plus a separate production build with persistent caches. Playwright is an explicit author-side browser tool. The live product spans Operator, Space, Time, Gallery/agent guestbook, proxy reporting, Workbench, Journal, agent access/contribution contracts, GitHub activity and isolated visual/interaction experiments.

## Refresh rule

When this file conflicts with a source repository, current implementation and current retained evidence win. Update this file. Current implementation and retained current evidence lead; archive benchmarks stay in their source records.