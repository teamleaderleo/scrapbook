---
{
  "id": "cmux-14768",
  "repository": "manaflow-ai/cmux",
  "kind": "pull-request",
  "number": 14768,
  "title": "Attribute a red guard to the change that broke it and say how to fix it",
  "status": "merged",
  "happenedAt": "2026-09-26",
  "recordedAt": "2026-09-26",
  "areas": [
    "ci",
    "observability",
    "developer-loop"
  ],
  "summary": "Fast guards no longer block merges, so a red guard on main needs an owner fast. ci-guard-attribution.yml runs after every CI fast guards or repository-variables run: it walks back to the last green run, re-runs only the failing step on each first-parent commit in between, names the culprit PR with the exact assertion, the likely fix and a one-line repro, keeps one tracking issue per red workflow, and offers mechanical fix patches.",
  "evidence": {
    "label": "CMUX PR 14768",
    "url": "https://redirect.github.com/manaflow-ai/cmux/pull/14768",
    "basis": "PR body: a hand-run replay, 'analyze --sha 90e8953 --log <fast guards at 90e8953> --baseline 90e8953~4', ran the failing step on 4 commits, found 90e8953 (#14724) first to fail, and took 92 s and 2 API calls. It was given the log and a hand-set 4-commit baseline, so the 92 s excludes the workflow_run trigger, runner start and log fetch, and ranges over 40 commits are bisected by halving. At 90e8953 the fix was a hint, not a verified patch. The dry-run report it would have posted, naming #14724 and the missing CI_SEED_KEEP_LOCAL_RUNNERS exemption, is in the PR's replay comment (issuecomment-5841155073). No numeric claim is made: there is no measured before time, and no live attribution has happened yet (no CI fast guards run on main failed in the controller's webhook feed from 00:42 to 04:42Z 09-26)."
  },
  "claims": []
}
---

# Why this record exists

Once guards stopped blocking merges (hq#721), a red guard on main turned every open PR's check red until someone found the cause. This is a capability record without a numeric claim. The replay time (92 s) is kept in the evidence as a reference for the first live attribution, which can then be recorded as a claim.
