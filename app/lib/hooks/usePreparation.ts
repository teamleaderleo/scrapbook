import { useQuery } from "react-query";

function useRelatedData(type: 'project' | 'block', id: string) {
//   return useQuery(['relatedData', type, id], () => fetchRelatedData(type, id), {
//     enabled: !!id,
// use a prepare()
//   });
}